version: '3.8'

services:
  mahesh-agro-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mahesh-agro-ai
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
